# 服务端口。修改后，Docker 对外端口也会一起改变。
PORT=3100

# 填写网站完整来源，例如 https://syncinema.example.com。
# 保持 * 表示允许所有来源。
CORS_ORIGIN=*

# 敏感词管理密码。留空时关闭管理入口。
SENSITIVE_ADMIN_PASSWORD=

# ICE_SERVERS_JSON 与下方 TURN_* 二选一；通常使用 TURN_* 更容易填写。
ICE_SERVERS_JSON=
TURN_URLS=
TURN_USERNAME=
TURN_CREDENTIAL=
